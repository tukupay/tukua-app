import 'package:flutter/material.dart';
class SmsSummary extends StatelessWidget {
  const SmsSummary({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('summary'),
    );
  }
}
